[
    "RULE: Import the 'requests' library before using it.",
    "RULE: Only give concise, directly relevant answers to the user\u2019s explicit question and avoid unsolicited, overly detailed plans or generic advice.",
    "RULE: Always set a valid tool_choice (or disable tool calls) before the model generates content that may invoke a tool.",
    "RULE: Throttle requests to stay below the 8000 tokens per minute limit.",
    "RULE: Do not request more tokens in a minute than the organization\u2019s TPM limit (e.g., keep requests below 8000 tokens per minute).",
    "RULE: Always set tool_choice to a specific tool or 'auto' before the model attempts to call a tool.",
    "RULE: Set the tool_choice to the intended tool before the model can call it; otherwise set tool_choice to 'none' to prevent unintended tool invocation.",
    "RULE: Always specify a tool_choice when a tool call is expected; do not allow the model to call a tool without an explicit tool_choice.",
    "RULE: Do not allow the model to call a tool unless tool_choice is set to \"auto\" or a specific tool is explicitly selected.",
    "RULE: Always specify the tool choice before invoking any tool.",
    "RULE: Ensure the tool_choice parameter is set to a specific tool before the model is allowed to invoke any tool, preventing calls when tool_choice is None.",
    "RULE: Always specify a tool_choice before generating a tool call to avoid invoking a tool when tool_choice is none.",
    "RULE: Always include the required tool in the tool list before invoking it.",
    "RULE: Always include the intended tool in the tool list before the model attempts to call it."
]